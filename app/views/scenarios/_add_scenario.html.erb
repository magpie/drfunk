<% remote_form_for(Scenario.new) do |f| %>
    <%= f.text_field :name, :class => "text-field" %>
    <%= f.hidden_field :plan_id, {:value => @plan.id} %>
    <%= submit_tag 'Add Scenario' %>
<% end %>
