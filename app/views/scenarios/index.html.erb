<% title @plan.name + " - Scenario List" %>
<% navigation(link_to('Test Plans', plans_path)) %>

<div id="plan">
  <%= render :partial => "plans/plan_name" %>
</div>
<br/>

<div id="add_feature">
  <%= render :partial => 'features/add_feature' -%>
</div>
<br/>
<div id="scenario_list">
  <%= render :partial => 'scenario_list'-%>
</div>

<% footer(
     link_to('Delete this plan', @plan, :confirm => 'Are you sure?', :method => :delete),
     link_to('Clear Results', 
       :url => clear_results_plan_path(@plan), 
       :method => :put,
       :confirm => "Clear test results for #{@plan.name}?",
       :remote => true), 
     link_to('HTML', @plan),
     link_to('XML', plan_path(@plan, :xml)),
     link_to('Test Failures', failures_plan_path(@plan))
) %> 
