unless @scenario.id.nil?
  page.replace_html "scenario_set_#{@feature.id}", :partial => 'scenario_set'
  page.visual_effect :highlight, "scenario_#{@scenario.id}"
  page["new_scenario_#{@feature.id}"].reset
end
