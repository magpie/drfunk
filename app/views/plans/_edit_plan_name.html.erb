<% remote_form_for(@plan) do |f| %>
<%= f.text_field :name, :value => @plan.name %>
<%= submit_tag 'Save'%> or
<%= link_to_remote('Cancel',
                   :url => plan_path(@plan),
                   :method => :get) %>
<% end %>
