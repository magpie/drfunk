<%= form_for(@plan, :remote => true) do |f| %>
<%= f.text_field :name, :value => @plan.name %>
<%= submit_tag 'Rename this plan'%> or
<%= link_to('Cancel',
                   plan_path(@plan),
                   :method => :get,
                   :remote => true) %>
<% end %>
