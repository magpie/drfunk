<span class="medium-text">Setup</span>
<% remote_form_for(@scenario.setup, 
     :url => scenario_setup_path(@scenario)) do |f| %>
    <%= f.text_area :content, {:rows => 10, :value => @scenario.setup.content}%><br/>
    <%= submit_tag 'Save this setup' %> or 
<%= link_to_remote('Cancel',
                   :url => scenario_setup_path(@scenario),
                   :html => {:class => "small-text"},
                   :method => :get) %>
<% end %>
